interface ChipProps {
    text: string;
    dismissable?: boolean; // should we show the 'x'?
    color: ""
}

const Chip = ({ text, dismissable }: ChipProps) => {

    return (
        <span>

        </span>
    );
};